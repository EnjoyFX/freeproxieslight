# One proxy-list source URL per line. Copy to domains.txt and edit.
# Lines that are blank or start with '#' are ignored by the CLI loader.
#
# HTTP/HTTPS sources (default). These share the same table layout and parse
# out of the box:
https://www.sslproxies.org/
https://free-proxy-list.net/
https://www.us-proxy.org/
https://www.google-proxy.net/
https://free-proxy-list.net/anonymous-proxy.html

# SOCKS sources. Keep these in a SEPARATE file and run with --socks, e.g.:
#   freeproxieslight -s socks.txt --socks -o socks_checked.txt
# Do not mix them with the HTTP list above: --socks applies one parser to
# every source, so HTTP pages would be misparsed (and vice versa).
# https://www.socks-proxy.net/
